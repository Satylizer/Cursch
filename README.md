# 🧠 HR RAG Assistant

HR-ассистент на базе **RAG (Retrieval-Augmented Generation)** для ответов на кадровые вопросы  
(отпуск, больничный, увольнение и т.д.) с использованием загруженных документов.

---

## 🛠 Технологический стек

- **FastAPI** — backend и API
- **LangChain** — оркестрация RAG-цепочки
- **ChromaDB** — векторное хранилище
- **Llama.cpp** + **Mistral 7B** — локальная LLM в формате GGUF
- **Telegram Bot (aiogram)** — интерфейс для пользователей

---

## 📦 Требования

### Система
- **Python 3.10+**
- **Git**
- **C++ compiler** (обязателен для сборки `llama.cpp`)
- **RAM**: 8 ГБ минимум, рекомендуется 16 ГБ
- **Свободное место на диске**: ~10 ГБ

> ✅ Проверка версии Python:
```bash
python --version
```

---

## 🚀 Развёртывание

### 1️⃣ Клонирование репозитория
```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd practice_project_0/practice_project/backend/app
```

### 2️⃣ Создание виртуального окружения
```bash
python -m venv venv
```

#### Активация:
- **Windows**:
  ```bash
  venv\Scripts\activate
  ```
- **Linux / macOS**:
  ```bash
  source venv/bin/activate
  ```

### 3️⃣ Установка зависимостей
```bash
pip install -r requirements.txt
```

> 💡 Если файла `requirements.txt` нет, сгенерируйте его:
> ```bash
> pip freeze > requirements.txt
> ```

---

## 🧠 Установка модели LLM

Используется локальная модель **Mistral 7B** в формате **GGUF**.

### Скачать модель:
Файл:  
`mistral-7b-instruct-v0.2.Q4_K_M.gguf`

> 📥 Скачать можно с [Hugging Face](https://huggingface.co/TheBloke/Mistral-7B-Instruct-v0.2-GGUF)

### Разместить по пути:
```
backend/mistral/mistral-7b-instruct-v0.2.Q4_K_M.gguf
```

### Проверить путь в коде (`rag_chain/rag_chain.py` или аналогичном):
```python
Agent.llm = LlamaCpp(
    model_path="../../mistral/mistral-7b-instruct-v0.2.Q4_K_M.gguf",
    n_ctx=2048,
    n_batch=512,
    verbose=False
)
```

---

## 🗂 Структура проекта

```
backend/
 └─ app/
     ├─ config/
     │   └─ config.py          # Настройки (токены, пути)
     ├─ rag_chain/
     │   └─ rag_chain.py       # Логика RAG-цепочки
     ├─ services/
     │   └─ chroma_service.py  # Работа с ChromaDB
     ├─ api/
     │   └─ routes.py          # FastAPI эндпоинты
     ├─ test/
     │   └─ check_rag.py       # Тестирование RAG
     ├─ main.py                # Запуск FastAPI
     └─ __init__.py
```

---

## 📚 Хранилище ChromaDB

- Все документы хранятся **локально**.
- Используется **персистентное хранилище** (`./chroma_db`).
- Уникальные ID документов формируются как `sha256(text)`.

### Очистка базы (при необходимости):
```python
import shutil
shutil.rmtree("./chroma_db")
```

---

## 🧪 Проверка RAG

### Запуск теста:
```bash
python test/check_rag.py
```
или
```bash
python -m test.check_rag
```

### Что делает тест:
1. Очищает существующую базу ChromaDB
2. Загружает тестовые документы (PDF/TXT/DOCX)
3. Индексирует их в векторное хранилище
4. Задаёт примеры вопросов (например: *«Как оформить отпуск?»*)
5. Проверяет, получает ли осмысленный ответ от модели

---

## 🌐 Запуск API

```bash
uvicorn main:app --reload
```

### Документация:
- **Swagger UI**: [http://localhost:8000/docs](http://localhost:8000/docs)
- **ReDoc**: [http://localhost:8000/redoc](http://localhost:8000/redoc)

> API предоставляет эндпоинт, например: `POST /query` с телом `{"question": "..."}`

---

## 🤖 Telegram-бот

### Возможности:
- Принимает документы (**PDF / DOCX / TXT**) от HR-специалистов
- Автоматически **синхронизирует** их с RAG-системой
- Позволяет сотрудникам задавать вопросы через чат
- Возвращает ответы **только на основе загруженных документов**

### Перед запуском:
1. Указать `BOT_TOKEN` в `config/config.py` или через `.env`:
   ```python
   BOT_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
   ```
2. Убедиться, что **API запущен** (бот делает запросы к локальному `localhost:8000`)

> 💡 Для продакшена: использовать ngrok или размещать API на сервере с публичным IP

---

## ⚠️ Особенности

| Особенность | Описание |
|------------|--------|
| **Однократная загрузка модели** | Модель Llama.cpp загружается один раз при старте приложения |
| **Первый запрос — медленный** | Из-за инициализации модели и векторной базы |
| **Ответы только по документам** | Если информация не найдена — возвращается **«Я не знаю»** |
| **Локальная работа** | Нет зависимости от облака, все данные остаются внутри компании |

# 🧠 HR RAG Assistant

HR-ассистент на базе **RAG (Retrieval-Augmented Generation)** для ответов на кадровые вопросы  
(отпуск, больничный, увольнение и т.д.) с использованием загруженных документов.

Технологический стек:
- FastAPI  
- LangChain  
- ChromaDB  
- Llama.cpp (Mistral 7B)  
- Telegram Bot (aiogram)

---

## 📦 Требования

### Система
- Python **3.10+**
- Git
- C++ compiler (обязателен для `llama.cpp`)
- RAM: **8 ГБ минимум**, рекомендуется 16 ГБ
- Свободное место на диске: ~10 ГБ

Проверка Python:
```bash
python --version
🚀 Развёртывание
1️⃣ Клонирование репозитория
bash
Копировать код
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd practice_project_0/practice_project/backend/app
2️⃣ Создание виртуального окружения
bash
Копировать код
python -m venv venv
Активация:

Windows

bash
Копировать код
venv\Scripts\activate
Linux / macOS

bash
Копировать код
source venv/bin/activate
3️⃣ Установка зависимостей
bash
Копировать код
pip install -r requirements.txt
Если файла нет:

bash
Копировать код
pip freeze > requirements.txt
🧠 Установка модели LLM
Используется локальная модель Mistral 7B в формате GGUF.

Скачать файл:

Копировать код
mistral-7b-instruct-v0.2.Q4_K_M.gguf
Разместить по пути:

bash
Копировать код
backend/mistral/mistral-7b-instruct-v0.2.Q4_K_M.gguf
Проверить путь в коде:

python
Копировать код
Agent.llm = LlamaCpp(
    model_path="../../mistral/mistral-7b-instruct-v0.2.Q4_K_M.gguf"
)
🗂 Структура проекта
arduino
Копировать код
backend/
 └─ app/
     ├─ config/
     │   └─ config.py
     ├─ rag_chain/
     │   └─ rag_chain.py
     ├─ services/
     │   └─ chroma_service.py
     ├─ api/
     │   └─ routes.py
     ├─ test/
     │   └─ check_rag.py
     ├─ main.py
     └─ __init__.py
📚 Хранилище ChromaDB
Все документы хранятся локально

Персистентное хранилище

Уникальные ID документов формируются как sha256(text)

Очистка базы:

python
Копировать код
import shutil
shutil.rmtree("./chroma_db")
🧪 Проверка RAG
Запуск теста:

bash
Копировать код
python test/check_rag.py
Или корректно как модуль:

bash
Копировать код
python -m test.check_rag
Тест:

очищает базу

загружает документы

задаёт вопросы

проверяет ответы модели

🌐 Запуск API
bash
Копировать код
uvicorn main:app --reload
Swagger UI:

bash
Копировать код
http://localhost:8000/docs
🤖 Telegram-бот
Загружает документы (PDF / DOCX / TXT)

Синхронизирует их с RAG

Позволяет задавать вопросы по документам

Перед запуском:

указать BOT_TOKEN

убедиться, что API запущен

⚠️ Особенности
Модель загружается один раз при старте

Первый запрос может быть медленным

Ответы формируются только на основе документов

При отсутствии ответа возвращается "Я не знаю"
